# Dockerfile for OpenSign Frontend (client)
FROM opensign/opensign:main
# If you need to add custom env or files, do so here
# Example: COPY .env.prod /usr/src/app/.env.prod
# ENTRYPOINT and CMD are inherited from the image